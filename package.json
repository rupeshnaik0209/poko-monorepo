{
  "name": "my-monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Rupesh",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "lerna-dockerize": "lerna-dockerize"
  },
  "dependencies": {
    "lerna-dockerize": "^1.0.9"
  },
  "devDependencies": {
    "lerna": "^7.4.2"
  }
}